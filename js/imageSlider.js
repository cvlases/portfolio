// JavaScript to handle the click event
document.getElementById('link').addEventListener('click', function(event) {
    // Ensure the click on the image wrapper navigates to the link
    const link = this.querySelector('a');
    if (link) {
        window.open(link.href, link.target || '_self');
    }
    });

document.getElementById('link2').addEventListener('click', function(event) {
    // Ensure the click on the image wrapper navigates to the link
    const link = this.querySelector('a');
    if (link) {
        window.open(link.href, link.target || '_self');
    }
    });

document.getElementById('link3').addEventListener('click', function(event) {
    // Ensure the click on the image wrapper navigates to the link
    const link = this.querySelector('a');
    if (link) {
        window.open(link.href, link.target || '_self');
    }
    });

document.getElementById('link4').addEventListener('click', function(event) {
// Ensure the click on the image wrapper navigates to the link
const link = this.querySelector('a');
if (link) {
    window.open(link.href, link.target || '_self');
}
});

document.getElementById('link5').addEventListener('click', function(event) {
    // Ensure the click on the image wrapper navigates to the link
    const link = this.querySelector('a');
    if (link) {
        window.open(link.href, link.target || '_self');
    }
    });

    document.getElementById('link5').addEventListener('click', function(event) {
        // Ensure the click on the image wrapper navigates to the link
        const link = this.querySelector('a');
        if (link) {
            window.open(link.href, link.target || '_self');
        }
        });

document.getElementById('link6').addEventListener('click', function(event) {
// Ensure the click on the image wrapper navigates to the link
const link = this.querySelector('a');
if (link) {
    window.open(link.href, link.target || '_self');
}
});

document.getElementById('link7').addEventListener('click', function(event) {
    // Ensure the click on the image wrapper navigates to the link
    const link = this.querySelector('a');
    if (link) {
        window.open(link.href, link.target || '_self');
    }
    });
    
document.getElementById('link8').addEventListener('click', function(event) {
    // Ensure the click on the image wrapper navigates to the link
    const link = this.querySelector('a');
    if (link) {
        window.open(link.href, link.target || '_self');
    }
    });

document.getElementById('link9').addEventListener('click', function(event) {
// Ensure the click on the image wrapper navigates to the link
const link = this.querySelector('a');
if (link) {
window.open(link.href, link.target || '_self');
}
});

document.getElementById('link10').addEventListener('click', function(event) {
// Ensure the click on the image wrapper navigates to the link
const link = this.querySelector('a');
if (link) {
    window.open(link.href, link.target || '_self');
}
});

document.getElementById('link11').addEventListener('click', function(event) {
    // Ensure the click on the image wrapper navigates to the link
    const link = this.querySelector('a');
    if (link) {
        window.open(link.href, link.target || '_self');
    }
    });